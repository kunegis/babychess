This directory contains source files which are used to test the presence of
certain library function.  Tests are done by ./configure and the results
written into config.hh, which is included using #include "../config.hh". 
